<% @books.each do |book| %>
  <div>
    
    <p><%= image_tag 'no_image.jpg' %></p>
    <%= link_to book_path(book.id) do %>
      <p> <%= book.title%> </p>
    <% end %>
    <p><%= book.opinion%> </p>
    <p><%= book.user.name %></p>
  </div>
<% end %>